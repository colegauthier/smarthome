CREATE TABLE account (
    id               varchar(500) primary key NOT NULL,
    username         varchar(500),
    password         varchar(500),
    email            varchar(500),
    role             varchar(500)
);